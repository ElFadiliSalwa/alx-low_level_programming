#include "main.h"
#include<stdlib.h>
/**
 * _abs - computes the absolute value of an integer
 * @n: the number to compute its absolute value
 *
 * Return: int
 */
int _abs(int n)
{
	return (abs(n));
}
